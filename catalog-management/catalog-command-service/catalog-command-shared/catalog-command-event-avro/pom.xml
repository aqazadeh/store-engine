<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>az.kon.academy.catalog</groupId>
        <artifactId>catalog-command-service</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>catalog-command-event-avro</artifactId>

    <dependencies>
        <dependency>
            <groupId>org.apache.avro</groupId>
            <artifactId>avro</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>1.6.2</version>
        </dependency>

        <dependency>
            <groupId>az.kon.academy.catalog.event</groupId>
            <artifactId>catalog-domain-event</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>az.kon.academy.avro</groupId>
                <artifactId>avro-event-maven-plugin</artifactId>
                <version>1.0.0</version>
                <executions>
                    <execution>
                        <id>generate-avsc</id>
                        <goals>
                            <goal>generate-avsc</goal>
                        </goals>
                        <configuration>
                            <eventBasePackage>az.kon.academy.catalog.event</eventBasePackage>
                            <outputDirectory>${project.basedir}/src/main/avro</outputDirectory>
                        </configuration>
                    </execution>
<!--                    <execution>-->
<!--                        <id>generate-mappers</id>-->
<!--                        <goals>-->
<!--                            <goal>generate-mappers</goal>-->
<!--                        </goals>-->
<!--                        <configuration>-->
<!--                            <avroDirectory>${project.basedir}/src/main/avro</avroDirectory>-->
<!--                            <outputDirectory>${project.basedir}/src/main/java</outputDirectory>-->
<!--                            <mapStructConfigClass>az.kon.academy.catalog.avro.mapper.common.AvroMapStructConfig</mapStructConfigClass>-->
<!--                        </configuration>-->
<!--                    </execution>-->
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.avro</groupId>
                <artifactId>avro-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>schema</goal>
                        </goals>
                        <configuration>
                            <sourceDirectory>${project.basedir}/src/main/avro/</sourceDirectory>
                            <outputDirectory>${project.basedir}/src/main/java/</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>

                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>1.6.2</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>